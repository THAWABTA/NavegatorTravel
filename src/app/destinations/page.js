import Destinations from "@/components/Destinations";

export const metadata = {
  title: "Destinations — Navigator Travel",
  description: "A cinematic journey through the world's most extraordinary places.",
};

export default function DestinationsPage() {
  return <Destinations />;
}